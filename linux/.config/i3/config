# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1
set $term st
set $browser firefox
set $browser2 google-chrome-stable

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: Hack Nerd Font 12

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 9

#exec --no-startup-id redshift -l 40:75

exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet
exec --no-startup-id feh --bg-fill --randomize --no-fehbg ~/.local/share/wallpapers
exec --no-startup-id thunar --daemon
#exec --no-startup-id picom

# use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# increase brightness
bindsym XF86MonBrightnessUp exec --no-startup-id doas /usr/bin/xbacklight -inc 10 && xbacklight -get | awk -F. '{ print $1 }' > ~/.cache/i3brightness && $refresh_i3status
# decrease brightness
bindsym XF86MonBrightnessDown exec --no-startup-id doas /usr/bin/xbacklight -dec 10 && xbacklight -get | awk -F. '{ print $1 }' > ~/.cache/i3brightness && $refresh_i3status

# use mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

for_window [instance="dropdown"] floating enable
for_window [instance="dropdown"] resize set 2500 1500
for_window [instance="dropdown"] move scratchpad
exec --no-startup-id $term -n dropdown

for_window [instance="sound"] floating enable
for_window [instance="sound"] resize set 2000 1000
for_window [instance="sound"] move scratchpad
exec --no-startup-id $term -n sound -e pulsemixer

for_window [instance="math"] floating enable
for_window [instance="math"] resize set 2000 2000
for_window [instance="math"] move scratchpad
exec --no-startup-id $term -n math -f mono:pixelsize=40 -e calc

for_window [Class="thunderbird"] floating enable
for_window [Class="thunderbird"] resize set 3000 2000
for_window [Class="thunderbird"] move scratchpad
exec --no-startup-id thunderbird

for_window [Class="KeePassXC"] floating enable
for_window [Class="KeePassXC"] resize set 3000 2000
for_window [Class="KeePassXC"] move scratchpad
exec --no-startup-id keepassxc

for_window [Class="Todoist"] floating enable
for_window [Class="Todoist"] resize set 2500 2000
for_window [Class="Todoist"] move scratchpad
exec --no-startup-id flatpak run com.todoist.Todoist

for_window [Class="Bluebubbles"] floating enable
for_window [Class="Bluebubbles"] resize set 2500 2000
for_window [Class="Bluebubbles"] move scratchpad
#exec --no-startup-id flatpak run app.bluebubbles.BlueBubbles

# custom keybinds
bindsym $mod+Return exec --no-startup-id $term
bindsym $mod+Shift+Return [instance="dropdown"] scratchpad show; [instance="dropdown"] move position center
bindsym $mod+a [instance="sound"] scratchpad show; [instance="sound"] move position center
bindsym $mod+c [instance="math"] scratchpad show; [instance="math"] move position center
bindsym $mod+q kill
bindsym $mod+d exec --no-startup-id dmenu_run -fn mono:size=12
bindsym $mod+w exec $browser
bindsym $mod+Shift+w exec $browser2
bindsym $mod+m [Class="thunderbird"] scratchpad show; [Class="thunderbird"] move position center
bindsym $mod+f exec thunar
bindsym $mod+e exec emacs
bindsym $mod+backslash [Class="KeePassXC"] scratchpad show; [Class="KeePassXC"] move position center
bindsym $mod+z exec --no-startup-id i3lock
bindsym $mod+p exec --no-startup-id feh --bg-fill --no-fehbg --randomize ~/.local/share/wallpapers
bindsym $mod+x exec --no-startup-id jetbrains-toolbox
bindsym $mod+o exec --no-startup-id libreoffice
bindsym $mod+Shift+o exec --no-startup-id flatpak run md.obsidian.Obsidian
bindsym $mod+Shift+d [Class="Todoist"] scratchpad show; [Class="Todoist"] move position center
bindsym $mod+Shift+i [Class="Bluebubbles"] scratchpad show; [Class="Bluebubbles"] move position center

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# use cursor keys to change output
bindsym $mod+Left focus output left
bindsym $mod+Down focus output down
bindsym $mod+Up focus output up
bindsym $mod+Right focus output right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# cursor keys move workspace to output
bindsym $mod+Shift+Left move workspace to output left
bindsym $mod+Shift+Down move workspace to output down
bindsym $mod+Shift+Up move workspace to output up
bindsym $mod+Shift+Right move workspace to output right

# split orientation
bindsym $mod+b split h
bindsym $mod+v split v

bindsym $mod+Shift+f fullscreen toggle

bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+g layout toggle split

bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

bar {
        status_command i3status
}
